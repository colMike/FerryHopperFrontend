import { Route } from '@angular/router';
import {AccessControlComponent} from './access-control.component';

export const accessControlRoutes: Route[] = [
    {
        path     : '',
        component: AccessControlComponent
    }
];
